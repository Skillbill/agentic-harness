# GSD context snapshot (2026-05-13T08:30:09.404Z)

## Top project memories
- [MEM004] (convention) PLAN.md frontmatter `context-needed: [stem1, stem2]` (YAML, stems matching ^[a-zA-Z0-9_-]+$, no .md, no path) is the single authoritative producer/consumer contract for per-task codebase context. ah-task-plan is the sole producer; ah-task-execute is the sole consumer; ah-task-discuss runs before PLAN exists and uses INDEX + on-demand judgment (with ARCHITETTURA+CONVENZIONI as discretionary fallback). Empty list `context-needed: []` is legal and must be emitted; missing key tolerated for back-compat but not produced by the template. Canonical spec: task-layout.md §3.3.
- [MEM008] (gotcha) Node 22.20.0 `--experimental-strip-types` does NOT rewrite `.js`→`.ts` in import specifiers (ERR_MODULE_NOT_FOUND). When a test/spec mandates a literal `'./foo.js'` import, the runtime sibling pattern is: keep `foo.ts` as canonical typed source for production code, and add `foo.js` with the same runtime implementation purely for specifier resolution. Used twice in M001/S03: `plan-context.{ts,js}` (T01) and `context-audit.{ts,js}` (T03). Future evolution must keep both in sync — or consolidate.
- [MEM001] (architecture) The agentic-harness extension injects context via `.pi/codebase/INDEX.md` (compact `path: summary` lines, <500 tokens) plus a `load_codebase_doc({name})` pi.registerTool, replacing forced full-body injection. INDEX.md on disk is authoritative; in-memory `buildCodebaseIndex` is the fallback. The injection is gated by a per-session one-shot cache (`codebaseContextInjected`).
- [MEM002] (gotcha) `load-codebase-doc.ts` does `import { Type } from "typebox"` at module scope, but the worktree has no package.json/node_modules — typebox lives only under the global PI host's `@earendil-works/pi-coding-agent/node_modules/`. Tests that import that module from the worktree root must symlink (or otherwise resolve) typebox first, or use a runtime preflight; see `tests/s01-load-on-demand.test.mts` for the self-h
…[truncated]
